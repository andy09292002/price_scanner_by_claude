# =========================
# Environment Variables Example
# =========================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# MongoDB Configuration (used by docker-compose)
MONGO_USERNAME=admin
MONGO_PASSWORD=changeme
MONGODB_DATABASE=mango

# MongoDB Configuration (used by Spring Boot directly, only needed for non-Docker local dev)
# MONGODB_URI=mongodb://username:password@localhost:27017/your_database

# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_BOT_USERNAME=YourBotUsername

# Optional: Override host-side server port (container always runs on 8080 internally)
# SERVER_PORT=8080

# Superstore API Configuration
SUPERSTORE_API_KEY=your-superstore-api-key
